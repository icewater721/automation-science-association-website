<section class="join-contact" id="join" aria-labelledby="join-title">
  <div class="join-inner">
    <div class="section-heading">
      <p>加入与联系</p>
      <h2 id="join-title">和我们一起，把想法做成成果</h2>
    </div>

    <div class="action-grid">
      <article>
        <span class="panel-code" aria-hidden="true">03-A / JOIN</span>
        <h3>加入我们</h3>
        <p>参与项目服务、赛事组织和内容运维，在实践中积累经验、认识伙伴。</p>
        <div class="panel-status">
          <span class="status-dot" aria-hidden="true"></span>2026 级招新预计于
          10 月上旬开展，以学院正式通知为准。
        </div>
      </article>

      <article id="contact">
        <span class="panel-code" aria-hidden="true">03-B / CONTACT</span>
        <h3>联系我们</h3>
        <p>赛事、大创、双创系统或科协活动相关问题，可通过官方答疑渠道咨询。</p>
        <div class="panel-links">
          <a href="/docs/">查看文档中心 <span aria-hidden="true">↗</span></a>
          <span class="pending">答疑群 / 邮箱待确认</span>
        </div>
      </article>
    </div>
  </div>
</section>

<style>
  .join-contact {
    padding-block: clamp(3.5rem, 7vw, 5.5rem);
  }

  .join-inner {
    width: min(76rem, calc(100% - 2rem));
    margin-inline: auto;
  }

  .section-heading {
    max-width: 44rem;
    margin-bottom: clamp(1.75rem, 4vw, 2.75rem);
  }

  .section-heading p {
    margin: 0 0 0.8rem;
    color: var(--color-accent);
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.08em;
  }

  h2 {
    margin: 0;
    font-size: clamp(2rem, 3.5vw, 3.15rem);
    line-height: 1.1;
    letter-spacing: -0.045em;
  }

  .action-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
  }

  article {
    display: grid;
    min-height: 16rem;
    align-content: start;
    padding: clamp(1.5rem, 4vw, 2.5rem);
    border: 1px solid var(--color-border);
    background: var(--color-surface);
  }

  h3 {
    margin: 0 0 1rem;
    font-size: clamp(1.65rem, 3vw, 2.3rem);
    letter-spacing: -0.04em;
  }

  .panel-code {
    display: none;
  }

  article > p {
    max-width: 32rem;
    margin: 0;
    color: var(--color-text-muted);
    line-height: 1.75;
  }

  .panel-status,
  .panel-links {
    align-self: end;
    margin-top: 2.5rem;
    padding-top: 1.25rem;
    border-top: 1px solid var(--color-border);
    font-size: 0.88rem;
  }

  .panel-status {
    display: flex;
    align-items: flex-start;
    gap: 0.7rem;
  }

  .status-dot {
    width: 0.5rem;
    height: 0.5rem;
    flex: 0 0 auto;
    margin-top: 0.3rem;
    border-radius: 50%;
    background: var(--color-accent);
  }

  .panel-links {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .panel-links a {
    font-weight: 700;
    text-decoration: none;
  }

  .panel-links a:hover {
    color: var(--color-accent);
  }

  .pending {
    color: var(--color-text-muted);
  }

  :global(:root[data-theme='ark']) .join-contact {
    background-image:
      linear-gradient(var(--color-border) 1px, transparent 1px),
      linear-gradient(90deg, var(--color-border) 1px, transparent 1px);
    background-size: 4rem 4rem;
  }

  :global(:root[data-theme='ark']) .section-heading p::before {
    margin-right: 0.55rem;
    content: '03 /';
  }

  :global(:root[data-theme='ark']) article {
    clip-path: polygon(
      0 0,
      calc(100% - 1.25rem) 0,
      100% 1.25rem,
      100% 100%,
      0 100%
    );
  }

  :global(:root[data-theme='ark']) .panel-code {
    display: block;
    margin-bottom: 1.5rem;
    color: var(--color-accent);
    font-family: ui-monospace, 'Cascadia Code', monospace;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.12em;
  }

  @media (max-width: 46rem) {
    .action-grid {
      grid-template-columns: 1fr;
    }

    article {
      min-height: auto;
    }
  }
</style>
