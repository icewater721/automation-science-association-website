<section class="join-contact" id="join" aria-labelledby="join-title">
  <div class="join-inner">
    <div class="section-heading">
      <p><span>03</span> Join / Contact</p>
      <h2 id="join-title">把你的好奇心，变成下一次行动</h2>
    </div>

    <div class="action-grid">
      <article>
        <p class="panel-code">JOIN US / 001</p>
        <h3>加入我们</h3>
        <p>
          无论你更关心科创项目、赛事组织还是内容运维，都能在这里找到一个参与和成长的位置。
        </p>
        <div class="panel-status">
          <span class="status-dot"></span>
          2026 级招新预计于 10 月上旬开展，以学院正式通知为准。
        </div>
      </article>

      <article id="contact">
        <p class="panel-code">CONTACT / 002</p>
        <h3>联系我们</h3>
        <p>
          如需咨询赛事、大创、双创系统或科协活动，可通过即将公布的官方答疑渠道联系我们。
        </p>
        <div class="panel-links">
          <a href="/docs/">查看文档中心 <span aria-hidden="true">↗</span></a>
          <span class="pending">答疑群 / 邮箱待确认</span>
        </div>
      </article>
    </div>
  </div>
</section>

<style>
  .join-contact {
    padding-block: clamp(4rem, 9vw, 7.5rem);
    background:
      linear-gradient(
        135deg,
        transparent 0 74%,
        color-mix(in srgb, var(--color-accent) 9%, transparent) 74%
      ),
      var(--color-surface);
    border-block: 1px solid var(--color-border);
  }

  .join-inner {
    width: min(76rem, calc(100% - 2rem));
    margin-inline: auto;
  }

  .section-heading {
    display: grid;
    max-width: 52rem;
    gap: 1rem;
    margin-bottom: clamp(2rem, 5vw, 3.75rem);
  }

  .section-heading p,
  .panel-code {
    margin: 0;
    color: var(--color-text-muted);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .section-heading p span,
  .panel-code {
    color: var(--color-accent);
  }

  .section-heading p span {
    margin-right: 0.75rem;
  }

  h2 {
    max-width: 14ch;
    margin: 0;
    font-size: clamp(2rem, 5vw, 4.5rem);
    line-height: 1.03;
    letter-spacing: -0.05em;
  }

  .action-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
  }

  article {
    display: grid;
    min-height: 23rem;
    align-content: start;
    padding: clamp(1.5rem, 4vw, 3rem);
    border: 1px solid var(--color-border);
    background: color-mix(in srgb, var(--color-page) 88%, transparent);
  }

  h3 {
    margin-block: 3.5rem 1rem;
    font-size: clamp(2rem, 4vw, 3rem);
    letter-spacing: -0.05em;
  }

  article > p:not(.panel-code) {
    max-width: 34rem;
    margin: 0;
    color: var(--color-text-muted);
    line-height: 1.75;
  }

  .panel-status,
  .panel-links {
    align-self: end;
    margin-top: 2.5rem;
    padding-top: 1.25rem;
    border-top: 1px solid var(--color-border);
    font-size: 0.88rem;
  }

  .panel-status {
    display: flex;
    align-items: flex-start;
    gap: 0.7rem;
  }

  .status-dot {
    width: 0.55rem;
    height: 0.55rem;
    flex: 0 0 auto;
    margin-top: 0.25rem;
    background: var(--color-accent);
  }

  .panel-links {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .panel-links a {
    font-weight: 700;
    text-decoration: none;
  }

  .panel-links a:hover {
    color: var(--color-accent);
  }

  .pending {
    color: var(--color-text-muted);
  }

  @media (max-width: 46rem) {
    .action-grid {
      grid-template-columns: 1fr;
    }

    article {
      min-height: auto;
    }
  }
</style>
